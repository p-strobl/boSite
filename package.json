{
  "name": "recipes",
  "version": "1.0.0",
  "description": "Recipes Site",
  "main": "index.js",
  "scripts": {
    "install:backend": "cd backend && npm install",
    "install:frontend": "cd frontend && npm install && run start",
    "start": "concurrently \"cd backend && server.js\" \"cd frontend && npm start\"",
    "postinstall": "run-p install:*"
  },
  "author": "Philipp Strobl",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.1"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
