import React from "react";

import "./BoundingBar.scss";

export function BoundingBar() {
  const uiClasses = {
    boundingBar: "BoundingBar",
  };

  return <div className={uiClasses.boundingBar} />;
}
