const landing = require('./landing');
const recipes = require('./recipes');
const remaining = require('./remaining');

module.exports = {
  landing: landing,
  recipes: recipes,
  remaining: remaining
};
